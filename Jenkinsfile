pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                echo "Building from GitHub..."
                sh 'terraform --versio'
            }
        }
    }
}
